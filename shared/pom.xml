<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.dark</groupId>
        <artifactId>javaHarness</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>javaHarness-shared</artifactId>
    <name>javaHarness :: shared</name>
    <description>API 契约层：CLI/server（及未来 web、app 端）共享的 DTO 与跨端枚举，纯 POJO 无框架依赖</description>

    <dependencies>
        <!-- ChatRequest 的 @NotBlank 等校验注解仅需 API 包编译（校验实现在 server 端由
             spring-boot-starter-validation 提供），版本由 spring-boot-starter-parent 的 BOM 管理 -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
        </dependency>
    </dependencies>
</project>

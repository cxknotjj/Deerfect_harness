# 构建上下文排除清单：BuildKit 按「<Dockerfile 文件名>.dockerignore」查找（须与 Dockerfile
# 同目录），路径相对 context（=项目根）。若 docker 版本过旧不识别本文件，改名为
# .dockerignore 放回项目根即可。

# 版本与本地工具链
.git
.idea
.vscode
.trae
.cli-history

# 本地 Maven 仓库（数百 MB，镜像内按 pom 重新解析到独立缓存层）
.mvn-repo

# 构建产物
target

# 文档（运行镜像不需要；*.md 仅匹配项目根，不影响 skills/、knowledge/ 内文档）
docs
*.md

# 不进 git 的本地配置（含 API key，禁止烘进镜像；需要时 compose 挂载）
mcp-config.json
channel/emojis/
